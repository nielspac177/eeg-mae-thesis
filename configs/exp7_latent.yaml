# Experiment 7 — t-SNE + UMAP map of the champion encoder's latent space.
# Champion: d256_h8 @ ep90. Marker colour = hard label; opacity = soft-label confidence.
# Set `pooling` to the winner of exp 6.
name: exp7_latent
kind: latent
enc_dim: 256
enc_heads: 8
pretrained: runs/pretrain/mae_d256_h8/snapshots/encoder_ep90.pt
pooling: cls
high_agreement_only: true
